package com.example.batteryrisk.repository;

import com.example.batteryrisk.domain.CollectionCursor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CollectionCursorRepository extends JpaRepository<CollectionCursor, String> {
}
